
Write a function named numberToString that takes the number num as an argument and returns a string containing that number by concatenating it with an empty string.

function numberToString(num){
  return num + '';
}
