
Write the isStringContainsNumber feature, which accepts the str line as an argument and returns true if the line contains a whole number, and is false, otherwise.

function isStringContainsNumber(str){
  return str == parseInt(str)? true: false;
}
