Write a function called isPropositionTrue that takes three integers a, b, c as arguments and returns true if the statement "The number b is between the numbers a and c" is true, and false otherwise.

function isPropositionTrue(a, b, c){
  if ( a < b && b < c || c < b && b < a ) return true;
  return false;
}
