апишите функцию с именем fillArray, которая принимает числа n (n >= 1) и x (1 <= x <= n) в качестве аргументов и возвращает массив из целых чисел от 1 до n включительно, за исключением числа x.
В решении необходимо использовать оператор continue.
function fillArray(n, x){
   let arr = [];
     for (let i = 1; i <= n; i++) {
      if (i === x) continue;
       arr.push(i);
     }
   return arr;  
}
