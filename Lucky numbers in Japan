The lucky numbers in Japan are "8", "3", "5", "7".
Write a function named luckyNumber that takes two numbers a, b as arguments and returns true if at least one of the given numbers is considered lucky in Japan, and false otherwise.


function luckyNumber(a, b){
  if (a === 8 || b === 8 || a === 3 || b === 3 || a === 5 || b === 5 || a === 7 || b === 7) return true;
  return false;
}
