Math.random () returns a random number in the range of 0 (inclusive) and 1 (not inclusive).
Write a feature named integerFrom0To9, which returns a random whole number from 0 to 9 inclusive.

function integerFrom0To9(){
  return Math.floor(Math.random() * 10);
}
