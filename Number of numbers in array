
Write the countNumbers feature, which accepts the arr array as an argument, and returns the number of items that have a "number" type.

function countNumbers(arr){
  let count = 0;
  for(let el of arr) {
    if (typeof(el) === 'number') count++;
  }
  return count;
}
