Write a min function that accepts the arr array as an argument and returns the minimum element of that array. If the array is empty, the function should return.
Examples:
the min function (7, 23, 34, 9, 4, 45) is to return 4.
the min function should be returned.

function min(arr){
 return arr.length === 0? []: Math.min(...arr);
}
