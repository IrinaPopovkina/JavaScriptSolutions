Three different numerical values ​​are given. If the values ​​are in ascending order, double each value; otherwise, assign the opposite value to each value.
Write a function called newValues ​​that takes three different numbers a, b, c as arguments and returns the changed values ​​as an array [a, b, c].

function newValues(a, b, c){
  if (a < b && b < c && c > a) {
    return [a * 2, b * 2, c * 2];
  } else {
    return [-a, -b, -c];
  }
}
