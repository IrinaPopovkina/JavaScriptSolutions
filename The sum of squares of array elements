
Write the sumOfSquares feature, which takes the arr array as an argument and returns the amount of squares of array items.
Use a loop in the solution. Array methods are not allowed.

function sumOfSquares(arr){
  let sum = 0;
  for(let i = 0; i < arr.length; i++) {
    sum += Math.pow(arr[i], 2);
  }
  return sum;
}
