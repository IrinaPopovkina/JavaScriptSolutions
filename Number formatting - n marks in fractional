Write a function nDecimalDigits that takes the numbers num (original number) and n (number of decimal places) as arguments and returns num with only n decimal places. 
The number does not need to be rounded just leave only n decimal places.
function nDecimalDigits(num, n){
 let a = Math.pow(10, n);
  return Math.trunc(num * a) / a;
}
