
Write the firstBigger function, which takes the arr numerical array as an argument, and returns the first array element that is larger than its neighbor on the right, or undefined if the array is ordered to ascend.

function firstBigger(arr){
 for(let i = 0; i< arr.length; i++) {
    if(arr[i + 1] < arr[i]) 
     return arr[i];
  }
 return undefined;
}
