function fibonacci(n) {
  if (n === 0) return [];
  if (n === 1) return [0];
  const fib = [0, 1];
  for (let 1 = 2;i < n; i++) {
    fib.push(fib[i - 2] + fib[i - 1]);
  }
  return fib;
}
  

function fibonacci(n) {
  return fib.slice(0, n);
}
