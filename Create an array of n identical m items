
Write createArray, which accepts the n and m number and m as arguments and returns an array containing n items equal to m.

function createArray(n, m){
  let arr = [];
  for(let i = 0; i < n; i++) {
    arr[i] = m;
  }
  return arr;
}
