
Write alternationArray, which accepts a whole non-negative n number as an argument and returns an array containing numbers from 0 to n, in which even numbers are positive and the odd negative.
function alternationArray(n){
  let arr = [];
  for(let i = 0; i <= n; i++) {
    if(i % 2 === 0) arr.push(i);
    else arr.push(-i);
  } 
  return arr;
}
  
