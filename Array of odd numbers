
Write createOddArray, which takes a whole non-negative n number as an argument and returns an array containing n odd numbers.

function createOddArray(n){
  let arr = [];
  for(let i = 0; i < n * 2; i++) {
    if (i % 2 !== 0) arr.push(i);
  }
  return arr;
}
