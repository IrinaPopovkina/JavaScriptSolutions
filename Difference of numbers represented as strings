You are given two lines containing numbers. Get the difference of numbers as a string.
Write a function called differenceOfStr that takes two numbers a and b as strings as arguments and returns a string containing the difference.

function differenceOfStr(a, b){
  let dif = (+a) - (+b);
  return String(dif);
}
